#include <stdio.h>
#include <string.h>

int main(void)
{
 char* input;
 char* token;
 char* splitter;
 scanf("%[^\n]", input);
/*
 splitter = input;

 int i = 0;
 while ((token = strtok_r(splitter, " ",  &splitter)))
	printf("%s\n", token);
}
*/
}
